name: Content Control Production DAG (Legacy Manual)
on:
  workflow_dispatch:
jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for diagnostics only. Canonical production orchestrator is episode1-max-speed.yml."
